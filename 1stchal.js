1 + 1
5 - 3
13 % 1
var favoriteNum = 13
var cel = 32
var fahr
fahr = cel * 9/5 + 32
function sub(num1, num2) {
  num1 - num2
}
function reverseCharAt(string,index) {
var length = string.length;
var realChar = string.charAt(index);
}

function reverseCharAt(string,index) {
string.length;
return .charAt(index);
}
function lifeTimeSupply(age, amount) {
  var daysRemaining = (82-age)*365;
  return amount*daysRemaining;
}

function celsiusToFahrenheit(cel) {
  var fahr = cel*(9/5)+32;
  console.log(fahr);
  } 
